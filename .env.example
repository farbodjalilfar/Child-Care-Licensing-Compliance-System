# Copy this file to .env and set a password before running `docker compose up -d`.
# SQL Server requires at least 8 characters with upper, lower, digit and symbol.
MSSQL_SA_PASSWORD=ChangeMe!Passw0rd
